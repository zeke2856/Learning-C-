#include <iostream>
#include <cmath>

int main(){
    double saving {-5000 } ;
    double weight = 7.7;
    std::cout << "Abs of weight is : " << std::abs(weight) << std::endl;
    std::cout << "Abs of saving is : " << std::abs(saving) << std::endl;
    
    double exponential = std::exp(10);
    std::cout << "The exponential is : " << exponential << std::endl;

}